name: stocks_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pandas
  - pyarrow
  - numpy
  - matplotlib
  - scikit-learn
  - scipy
  - sqlalchemy
  - requests
  - pip
  - pytorch
  - ipykernel
  - pip:
      - alpaca-py
      - beautifulsoup4
      - numba
      - pydantic
      - python-dotenv
      - shap
      - tqdm
      - yfinance
      - transformers
      - psycopg2-binary